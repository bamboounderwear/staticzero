{
  "name": "static-site-generator",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "main": "build.js",
  "engines": {
    "node": ">=18.0.0"
  }
}
